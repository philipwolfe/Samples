FilePicker Directory
===========================

Contains sources for AWS FilePicker's file menager that invoked when
user clicks the Browse button. 
To build assembly of codebehind file, run Build.bat file. It not only 
will build the dll, but also will place it into the /bin directory 
(..\..\bin). You can place the assembly into the /bin subdirectory 
of your web application.