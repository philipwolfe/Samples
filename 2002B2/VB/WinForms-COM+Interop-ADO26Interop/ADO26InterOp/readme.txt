Visual Basic.NET - ADO 2.6 InterOp Example


Note: This sample requires that the included AddressBook.mdb (Access Database) file reside in
the bin directory if running from the development environment.  If running the compiled .exe,
the AddressBook.mdb file must reside in same directory as the .exe.

Note: A C++ runtime error is generated when the form is closed.

