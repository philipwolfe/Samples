CmdBrowse is an Add-in that walks the command bar structure in the environment, displaying the
GUID/ID par of each command, along with it's named command.

To use this Add-in, build the VB6 project into a DLL. Then double-click the .reg file to register 
the Add-in as a VS7 Add-in.